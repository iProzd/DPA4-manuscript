# DPA4 Manuscript

- `archi_dpa4.tex` — canonical architecture summary for DPA4 (derived from source).
- `archi_eqv3.tex` — same role for EquiformerV3.
  Consult these before answering architecture questions.
- `main.tex` includes `chap/*.tex`; `sm.tex` is the supplement.
- LaTeX: `\S` is the section sign — use the project's `\Sph` macro for the sphere $S^2$.
- LaTeX: leave aux/log/toc/out files in place between `pdflatex` passes.
